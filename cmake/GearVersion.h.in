#ifndef GearVersion_h
#define GearVersion_h 1

// version macros
#define GEAR_MAJOR_VERSION @GEAR_VERSION_MAJOR@
#define GEAR_MINOR_VERSION @GEAR_VERSION_MINOR@
#define GEAR_PATCH_LEVEL @GEAR_VERSION_PATCH@
//#define GEAR_VERSION_GE( MAJV , MINV , PLEV)  ( (  GEAR_MAJOR_VERSION > MAJV ) || ( (GEAR_MAJOR_VERSION==MAJV) && ( GEAR_MINOR_VERSION > MINV ) ) || ( (GEAR_MAJOR_VERSION==MAJV) && ( GEAR_MINOR_VERSION == MINV ) && ( GEAR_PATCH_LEVEL >= PLEV ) ))
#define GEAR_VERSION_GE( MAJV , MINV )  ( (  GEAR_MAJOR_VERSION  > MAJV ) || ( (GEAR_MAJOR_VERSION==MAJV) && ( GEAR_MINOR_VERSION >= MINV ) ) )

#endif
